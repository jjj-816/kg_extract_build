# 长宁 H3 平台泡排工艺施工：抽取效果评估报告

## 1. 评估对象与可复现信息

- **模型运行**：`kg-run-20260714-103045`
- **run_id**：`94382ce5-0aa8-4d92-b55e-8794bc0e6cd5`
- **运行状态**：`completed`
- **方法配置**：DeepSeek `deepseek-v4-flash`，`shared_context_batch`；最小证据重叠 0.4，单批最多 3 个实体，上下文上限 8,000 字符。
- **Gold**：`MinerU_markdown_长宁H3平台泡排工艺施工_预处理.json`，文档级人工复核 Gold（395 条三元组）。
- **Gold SHA-256**：`bcb1ac67bf09b97e2094a0ef06fb1fad2605b577bb4fb8f17766626fad0c2631`
- **模型最终三元组数**：787。
- **文档匹配**：模型文档名与 Gold 文档名完全一致，无缺失或额外文档。
- **结果已保存**：MySQL `kg_evaluation_run`，`evaluation_id=2b8b7d01-7928-4bfe-b7d2-936ef85a3fcd`。

## 2. 本次计算口径

本报告使用项目现有评估模块的**严格五元组匹配**：

```text
(head, head_type, relation, tail, tail_type)
```

实体类型与关系名称先按当前 Schema 标准化，再进行集合去重和一对一精确匹配。该口径对应实验方案中的“严格 Gold”诊断指标。

> 重要：Gold 中有 canonical ID，但当前 MySQL 最终三元组表和评估模块均以实体文本及类型进行匹配，尚未保存/使用预测端的 canonical ID。因此，方案规定的“规范化三元组 `(head_canonical_id, relation, tail_canonical_id)` 主指标”在本次运行中**不可计算**，不能将下列严格 F1 当作规范化主指标。

## 3. 结果

| 指标 | TP | FP | FN | Precision | Recall | F1 |
|---|---:|---:|---:|---:|---:|---:|
| 实体（名称 + 类型） | 77 | 653 | 206 | 10.55% | 27.21% | 15.20% |
| 关系（头实体 + 关系 + 尾实体，不含类型） | 16 | 770 | 379 | 2.04% | 4.05% | 2.71% |
| 三元组（严格五元组） | 16 | 771 | 379 | **2.03%** | **4.05%** | **2.71%** |

辅助诊断：

- Schema 非法关系率：0 / 787（0.00%）。这说明输出通过了当前 Schema 类型约束，但不表示事实正确。
- 项目现有代码报告的证据覆盖率：787 / 787（100%），无证据率 0%。该实现只要尾实体字符串出现在整篇原文或任一检索句中就视为“有证据”，并非方案要求的“该三元组有至少一个直接支持证据句”；因此该数值仅作运行诊断，不应作为论文中的证据支持率。

## 4. 误差概览

模型输出比 Gold 多 392 条严格去重后三元组，且只精确命中 16 条。按关系类型观察：

- 命中最多：`TRIGGERS_RISK` 7 条、`USES_EQUIPMENT` 4 条、`PRECEDES` 2 条。
- 主要误报：`HAS_PARAMETER` 167 条、`TRIGGERS_RISK` 133 条、`RESPONSIBLE_FOR` 70 条、`USES_EQUIPMENT` 51 条。
- 主要漏报：`MITIGATED_BY` 102 条、`TRIGGERS_RISK` 43 条、`RECORDED_IN` 35 条、`HAS_PARAMETER` 27 条、`HAS_THRESHOLD` 25 条。

这表明当前方法能生成大量 Schema 合法候选，但与 Gold 的实体粒度、规范名称及关系边界明显不一致。尤其是风险—控制措施、记录、参数/阈值关系是本案例的主要缺口。

## 5. 与实验方案的一致性及结论边界

本次仅有 1 篇文档、1 次运行，故属于方案中定义的**案例实验**：可报告逐篇严格指标与误差分析，但不能据此声称方法可泛化，亦不能计算跨文档 Bootstrap 置信区间、方法差异显著性、盲评事实精度或 Cohen's Kappa。

若要形成方案中的主结果表，下一步应：

1. 在 MySQL 保存模型实体的 canonical ID、别名映射和三元组证据句 ID；以 `(head_canonical_id, relation, tail_canonical_id)` 计算规范化 P/R/F1。
2. 将证据支持判定改为“Gold/预测三元组对应的直接证据句是否匹配”，而不是尾实体是否出现于全文。
3. 冻结至少 5–10 篇测试文档，按 R2–R6 每篇独立运行 3 次；再报告逐篇结果、均值/标准差和文档级 Bootstrap 95% CI。
4. 对预测并集进行双人盲评，并单列实体提及、类型、规范化、别名聚类（E-1 至 E-5）和 Oracle 诊断指标。

